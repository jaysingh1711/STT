{
  "name": "mediscribe-voice-to-notes",
  "version": "1.0.0",
  "private": true,
  "description": "Voice-to-Notes real-time clinical dictation module (frontend).",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build"
  },
  "devDependencies": {
    "react-scripts": "5.0.1"
  },
  "notes": "No extra WebSocket library needed -- the native browser WebSocket API is used directly in services/websocketService.js. If integrating into an existing CRA/Vite/Next app, just copy the components/, services/, App.jsx and App.css files in; no new dependencies beyond React itself are required."
}
